<Window x:Class="ClientChecklistManager.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Client Checklist Manager"
        Height="680" Width="960"
        MinHeight="500" MinWidth="700"
        WindowStartupLocation="CenterScreen"
        Background="White">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource PrimaryBrush}" Padding="20,14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="Client Checklist Manager"
                           Foreground="White" FontSize="20" FontWeight="SemiBold"
                           VerticalAlignment="Center" />
                <TextBlock Grid.Column="1"
                           Text="{Binding ClientCount, StringFormat='{}{0} clients'}"
                           Foreground="#BBDEFB" FontSize="13"
                           VerticalAlignment="Center" />
            </Grid>
        </Border>

        <!-- Toolbar: Search + Add -->
        <Border Grid.Row="1" Padding="16,12" Background="{StaticResource LightGrayBrush}"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="16" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0"
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource StyledTextBox}"
                         VerticalContentAlignment="Center" />
                <TextBlock Grid.Column="0" Text="Search by ID, name, or email..."
                           Foreground="#9CA3AF" FontSize="13" Margin="12,0,0,0"
                           VerticalAlignment="Center" IsHitTestVisible="False">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SearchText}" Value="">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <Button Grid.Column="1" Content="Clear" Margin="8,0,0,0"
                        Style="{StaticResource SecondaryButton}"
                        Command="{Binding ClearSearchCommand}" />

                <Button Grid.Column="2" Content="Refresh" Margin="8,0,0,0"
                        Style="{StaticResource SecondaryButton}"
                        Command="{Binding RefreshCommand}" />

                <Button Grid.Column="3" Content="Settings" Margin="8,0,0,0"
                        Style="{StaticResource SecondaryButton}"
                        Click="Settings_Click" />

                <Button Grid.Column="5" Content="+ Add Client"
                        Style="{StaticResource PrimaryButton}"
                        Command="{Binding ShowAddClientCommand}" />
            </Grid>
        </Border>

        <!-- Add Client Panel (shown when IsAddingClient) -->
        <Border Grid.Row="2" Background="#FEFCE8" Padding="16" VerticalAlignment="Top"
                BorderBrush="#FDE68A" BorderThickness="0,0,0,1"
                Visibility="{Binding IsAddingClient, Converter={StaticResource BoolToVisibility}}"
                Panel.ZIndex="10">
            <StackPanel>
                <TextBlock Text="Add New Client" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,10" />
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="12" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="12" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="12" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Client ID *" FontSize="12"
                               Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4" />
                    <TextBox Grid.Row="1" Grid.Column="0" Style="{StaticResource StyledTextBox}"
                             Text="{Binding NewClientId, UpdateSourceTrigger=PropertyChanged}" />

                    <TextBlock Grid.Row="0" Grid.Column="2" Text="First Name" FontSize="12"
                               Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4" />
                    <TextBox Grid.Row="1" Grid.Column="2" Style="{StaticResource StyledTextBox}"
                             Text="{Binding NewClientFirstName, UpdateSourceTrigger=PropertyChanged}" />

                    <TextBlock Grid.Row="0" Grid.Column="4" Text="Last Name *" FontSize="12"
                               Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4" />
                    <TextBox Grid.Row="1" Grid.Column="4" Style="{StaticResource StyledTextBox}"
                             Text="{Binding NewClientLastName, UpdateSourceTrigger=PropertyChanged}" />

                    <TextBlock Grid.Row="0" Grid.Column="6" Text="Email" FontSize="12"
                               Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4" />
                    <TextBox Grid.Row="1" Grid.Column="6" Style="{StaticResource StyledTextBox}"
                             Text="{Binding NewClientEmail, UpdateSourceTrigger=PropertyChanged}" />
                </Grid>
                <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                    <Button Content="Save Client" Style="{StaticResource PrimaryButton}"
                            Command="{Binding AddClientCommand}" />
                    <Button Content="Cancel" Style="{StaticResource SecondaryButton}" Margin="8,0,0,0"
                            Command="{Binding CancelAddClientCommand}" />
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Client List -->
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Clients}"
                  SelectedItem="{Binding SelectedClient}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  HorizontalGridLinesBrush="#F3F4F6"
                  RowHeight="40"
                  ColumnHeaderHeight="36"
                  FontSize="13"
                  BorderThickness="0"
                  Background="White"
                  CanUserResizeRows="False">

            <DataGrid.InputBindings>
                <MouseBinding MouseAction="LeftDoubleClick"
                              Command="{Binding OpenClientCommand}" />
            </DataGrid.InputBindings>

            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="{StaticResource LightGrayBrush}" />
                    <Setter Property="Padding" Value="12,8" />
                    <Setter Property="FontWeight" Value="SemiBold" />
                    <Setter Property="FontSize" Value="12" />
                    <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}" />
                    <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
                    <Setter Property="BorderThickness" Value="0,0,0,1" />
                </Style>
            </DataGrid.ColumnHeaderStyle>

            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Setter Property="Cursor" Value="Hand" />
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#F9FAFB" />
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#EFF6FF" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>

            <DataGrid.CellStyle>
                <Style TargetType="DataGridCell">
                    <Setter Property="Padding" Value="12,0" />
                    <Setter Property="BorderThickness" Value="0" />
                    <Setter Property="VerticalAlignment" Value="Center" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="DataGridCell">
                                <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}">
                                    <ContentPresenter VerticalAlignment="Center" />
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.CellStyle>

            <DataGrid.Columns>
                <DataGridTemplateColumn Header="" Width="30">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Ellipse Width="10" Height="10" HorizontalAlignment="Center"
                                     Fill="{Binding LastEmailed, Converter={StaticResource FollowUpIndicator}}"
                                     ToolTip="Needs follow-up" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="CLIENT ID" Binding="{Binding ClientId}" Width="140" />
                <DataGridTextColumn Header="LAST NAME" Binding="{Binding LastName}" Width="*" />
                <DataGridTextColumn Header="FIRST NAME" Binding="{Binding FirstName}" Width="*" />
                <DataGridTextColumn Header="EMAIL" Binding="{Binding Email}" Width="*" />
                <DataGridTextColumn Header="LAST EMAILED"
                                    Binding="{Binding LastEmailed, Converter={StaticResource DateTimeFormat}}"
                                    Width="160" />
            </DataGrid.Columns>
        </DataGrid>

        <!-- Status bar -->
        <Border Grid.Row="3" Background="{StaticResource LightGrayBrush}" Padding="16,8"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0">
            <TextBlock Text="{Binding StatusMessage}"
                       Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" />
        </Border>
    </Grid>
</Window>
