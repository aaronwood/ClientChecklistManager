<Window x:Class="ClientChecklistManager.Views.TaxYearSelectDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding ClientName, StringFormat='Select Tax Year - {0}'}"
        Height="320" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="White">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Heading -->
        <TextBlock Grid.Row="0" Text="Select a tax year to work with:"
                   FontSize="14" FontWeight="SemiBold" Margin="0,0,0,12" />

        <!-- Tax Year ComboBox -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Tax Year:" VerticalAlignment="Center"
                       FontSize="13" Margin="0,0,12,0" />
            <ComboBox Grid.Column="1" ItemsSource="{Binding AvailableYears}"
                      SelectedItem="{Binding SelectedTaxYear}"
                      FontSize="14" Padding="8,6" />
        </Grid>

        <!-- Existing years label -->
        <TextBlock Grid.Row="2" Text="Years with existing data for this client:"
                   FontSize="12" Foreground="#6B7280" Margin="0,0,0,6" />

        <!-- Existing years list -->
        <Border Grid.Row="3" BorderBrush="#D1D5DB" BorderThickness="1" CornerRadius="4" Padding="8">
            <ItemsControl ItemsSource="{Binding ExistingYears}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#DBEAFE" CornerRadius="10" Padding="10,4" Margin="0,0,6,4">
                            <TextBlock Text="{Binding}" FontSize="12" Foreground="#1E40AF" />
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <Border.Style>
                <Style TargetType="Border">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding ExistingYears.Count}" Value="0">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
        </Border>

        <TextBlock Grid.Row="3" Text="No existing data for this client yet."
                   FontSize="12" Foreground="#9CA3AF" FontStyle="Italic"
                   VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Setter Property="Visibility" Value="Collapsed" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding ExistingYears.Count}" Value="0">
                            <Setter Property="Visibility" Value="Visible" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>

        <!-- Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Cancel" Style="{StaticResource SecondaryButton}"
                    Click="Cancel_Click" Margin="0,0,8,0" />
            <Button Content="Open" Style="{StaticResource PrimaryButton}"
                    Click="Open_Click" />
        </StackPanel>
    </Grid>
</Window>
